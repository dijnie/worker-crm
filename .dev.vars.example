# Local development only. Generate your own secret; never reuse a production secret.
# See README.md for setup.
BETTER_AUTH_SECRET=
AUTH_BASE_URL=http://localhost:3000
AUTH_EMAIL_FROM=noreply@example.invalid
