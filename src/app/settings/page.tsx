import { AppEmptyState } from "@/components/app/app-empty-state";

export default function SettingsPage() {
  return (
    <AppEmptyState
      title="Settings"
      description="Application settings are not available yet."
    />
  );
}
